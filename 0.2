# 0.2
#define F_CPU 200000000L
#include <avr/io.h>
#include <avr/delay.h>


int main(void)
{
    /* Replace with your application code */
	DDRB=0x20; //PB5 output
	PORTB=0x01; //set PB0 high
	
    while (1) 
    {
		if(!(PINB & 0x01))
		{
			PORTB |=0x20; // if PB0 is connected to the ground,then set PB5 high
		}
		else
		{
			PORTB &= 0xDF;// if PB0 is not connected to the ground,then set PB5 low
		}                
    }
}
